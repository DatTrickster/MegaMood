# Copy this file to android-release-signing.properties and fill in your values.
# Do not commit android-release-signing.properties or your keystore file.
#
# storeFile: path to your .keystore or .jks file (relative to project root, or absolute)
# Example: release.keystore (if the keystore is in the project root)
storeFile=release.keystore
storePassword=your-store-password
keyAlias=your-key-alias
keyPassword=your-key-password
